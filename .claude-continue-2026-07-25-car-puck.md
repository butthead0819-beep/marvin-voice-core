# 車 puck 音質修復 — 明天續修

昨晚（2026-07-25 深夜~清晨）已修好並推上 main（commit `33e3d3c`）：
- Mac mixer 中段爆音 → 整首預解碼（`PreloadedF32MusicSource`）
- DJ Tail 轉場中斷 → 點火時背景 preload 下一首，零等待換源
- CPU 排程整段中斷 → `audioPlaybackTask` 移 core 1 + 優先權拉高
- `/audio_stream` 封包批次化（100ms batch）、TTS 音量 0.3→0.2

記憶：`project_car_puck_pops_full_fix_2026-07-25.md`（MEMORY.md 已收錄索引）。

## 今天要接著查的兩個真問題

### 1. lwIP `pbuf_free` assert 導致整機重開機（未解，優先度最高）

序列埠實測抓到：
```
assert failed: pbuf_free /IDF/components/lwip/lwip/src/core/pbuf.c:755 (pbuf_free: p->ref > 0)
```
黃燈→綠燈閃＝真的在重開機，不是單純 WiFi/TCP 重連。根因推測：`audioNetworkTask`
（core 0，長駐 /audio_stream）跟 Arduino loop()（core 1，心跳/PTT/reply 各自開
WiFiClient/HTTPClient）同時碰 lwIP 堆疊、沒有互斥保護，pbuf 引用計數被兩邊同時
動到。

**昨晚試過一次全域 mutex（鎖住所有 socket I/O），結果鎖的粒度沒抓對，直接把板子
鎖死在開機黃燈（task_wdt 卡在 audioNet、IDLE0 餓死），已經整個撤掉還原。**
今天重新設計前，先想清楚：
- 到底是哪兩個具體操作在互踩？（連線建立/銷毀？還是任意讀寫都會撞？）
- 鎖的持有時間要多短——絕對不能鎖住整個長駐串流迴圈（心跳會被餓死）
- 白天有更多時間可以用 `STREAM_DEBUG_PRINT` 配合序列埠慢慢對照，不用像昨晚趕時間

### 2. 佇列播完會直接停播（使用者已確認要修，先擱置到現在）

`_run_tail_dj` 點火時若 `stream_queue` 還是空的（autopilot 還沒排入下一首），
退回舊行為；如果這首播完當下佇列依然空，`Stream Loop` 直接印「佇列播放完畢」
收攤。應該要佇列永遠不空——可能是 autopilot 補位邏輯的時機問題，或需要在
「播完但空佇列」這個 case 加一個保底立即補位。

## 現場狀態

- firmware 目前是**真值版本**（WiFi/token/IP 都填了），git 裡是佔位符版本
  （commit 前已還原、commit 後又填回真值到工作目錄）——**下次要 commit 記得
  再換回佔位符**（見 `mk2_esp32_puck_bringup.md` 燒錄鐵則）。
- Mac 區網 IP 現在是 192.168.1.114（DHCP 會變，燒錄前用 `ipconfig getifaddr en0`
  重新確認）。
- 板子 USB 路徑：`/dev/cu.usbmodem1101`；FQBN：
  `esp32:esp32:esp32s3:PSRAM=opi,FlashSize=16M,CDCOnBoot=cdc,USBMode=hwcdc,PartitionScheme=app3M_fat9M_16MB`
- 燒錄後**一定要手動按 RESET**（已知雷，軟重置常常不夠）。
