# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# Virtual environments
venv_simon/
venv/
.venv/
env/

# Audio files
*.wav
*.mp3
*.ogg
*.pipe

# Secrets — never commit
.env

# Logs — large, runtime-generated
*.log
*.log.*

# Daily logs (large, regenerated)
records/daily/

# Memory — personal player data, never publish
suki_memory.json
suki_memory.*.bak
suki_dna.json
suki_budget.json

# SQLite databases — runtime data, never commit
*.db
*.db-shm
*.db-wal

# Runtime data — personal and generated
game_log.jsonl
music_memory.json
wake_stats.json
departure_stats.json
bot_main.py.pid
consent.json
data/voice_presence.jsonl
data/bad_cover_blacklist.json
location_state.json
now_playing_state.json

# Records directory — personal transcripts and player data
records/

# Compiled STT binary
macos_stt_bin

# Claude Code local settings
.claude/

# Personal auto-memory index
MEMORY.md

# Dev scratch files
scratch/
task.md

# Build artifacts
*.egg-info/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
*.swp
*.swo
.env.github
.gstack/
.chroma_db/

# ErrorDispatcher incident reports (per-machine forensic logs)
.claude_todo/

# Reboot state — runtime metadata written on every restart
.marvin_reboot_state.json

# 機密：免費 API key（手動保存，勿提交）
Free API key/

# 日記漫畫產生物（generated，勿入歷史，含 ~9MB bundle）
marvin_comics*.html
.loop/
